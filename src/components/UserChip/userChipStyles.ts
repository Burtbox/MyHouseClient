
import { createStyles, Theme } from '@material-ui/core/styles';

const userChipStyles = (theme: Theme) => createStyles({
    chip: {
        display: 'inline-flex',
        marginRight: '4px',
    },
});

export default userChipStyles;
